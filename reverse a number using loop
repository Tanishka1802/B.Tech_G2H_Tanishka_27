#include <stdio.h>

int main()
{
    int n,x;
    int rev=0;
    printf("enter number %d \n");
    scanf("%d", &n);
    while(n>0)
    {
        int x=n%10;
        rev=rev*10+x;
        n=n/10;
    }
    printf("reversed number is %d",rev);
    
    
    return 0;
}
