#include <stdio.h>

int main() {
    int x,y,z;
    printf("enter value of first number");
    scanf("%d",&x);
    printf("enter value of second number");
    scanf("%d", &y);
    z=x;
    x=y;
    y=z;
    printf("%d,%d",x,y);

    return 0;
}
