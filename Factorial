#include <stdio.h>

int main()
{
    int a,i;
    int x=1;
    printf("enter number %d \n");
    scanf("%d", &a);
    for(i=1;i<=a;i++)
    {
        x=x*i;
    }
    printf("factorial of given number is  %d \n", x);
    
    return 0;
}
